{
  "name": "techtalk-2021-05",
  "version": "0.9.0",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:pdf && npm run build:pptx",
    "build:pdf": "marp --pdf --allow-local-files --output ../../out/ -I ./",
    "build:pptx": "marp --pptx --allow-local-files --output ../../out/ -I ./",    "build:html": "marp --allow-local-files --output ../../dist/osc21hi -I ./ && cp -r images ../../dist/osc21hi/images",
    "dev": "marp -swp --allow-local-files -I ../../"
  },
  "license": "MIT",
  "devDependencies": {
    "@marp-team/marp-cli": "^1.1.1"
  }
}
